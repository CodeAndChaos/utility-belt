{
  "name": "utility-belt",
  "version": "0.0.1",
  "main": "dist/index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "esbuild": "^0.8.50",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.5"
  },
  "scripts": {
    "test": "jest --watch",
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js"
  }
}
